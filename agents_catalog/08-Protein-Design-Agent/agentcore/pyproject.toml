[project]
name = "08-Protein-Design-Agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "bedrock-agentcore >= 1.0.3",
    "boto3 >= 1.38.0",
    "strands-agents >= 1.18.0",
    "strands-agents-tools >= 0.2.16",
]
